export class LoggingService {
  logStatusChange(status: string) {
    console.log(
      "🚀 ~ file: logging.service.ts ~ line 3 ~ LoggingService ~ logStatusChange ~ status. The status has changed to, ",
      status
    );
  }
}
